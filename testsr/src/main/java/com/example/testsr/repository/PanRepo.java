package com.example.testsr.repository;

import com.example.testsr.model.Pan;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PanRepo extends JpaRepository<Pan, Integer> {
}
